---
title: "「エクセル一括貼り付け for kintone」v1.1.0 公開：複数のサブテーブルへの貼り付けに対応"
description: "エクセル一括貼り付け for kintone を v1.1.0 にアップデート。1つのアプリに複数のサブテーブルがある場合でも、テーブルごとに列の対応・既存行の扱いを個別に設定できるようになりました。貼り付け時はセルのある表を自動で判別し、そのテーブルにだけ流し込みます。見積・注文の明細に付帯費用・作業明細などを並べた運用に最適です。"
pubDate: 2026-07-17
category: update
ogImage: "/images/excel-paste-banner.png"
---

kintone プラグイン「**エクセル一括貼り付け for kintone**」を **v1.1.0** にアップデートしました。1つのアプリに**複数のサブテーブル**を置いている場合でも、テーブルごとに貼り付け設定ができるようになりました。

## 今回のアップデート内容

- **複数のサブテーブルに対応** — 設定画面でテーブルごとの「カード」を追加し、**対象サブテーブル・列の対応方法（順番で対応／貼り付け時に指定）・既存行の扱い（下に追加／全て置き換え）**をテーブルごとに個別に設定できます。テーブル数の上限はありません
- **貼り付け先を自動で判別** — 作成・編集画面で貼り付けると、セルのある表を自動で見分けて、そのテーブルにだけ流し込みます。見積明細・付帯費用・作業明細のように役割の違う表を、それぞれ別の Excel から貼り付ける運用がそのまま行えます（同じ列構成の表が複数あるときだけ、貼り付け先を1回確認します）
- **重複設定の防止** — あるカードで選択済みのサブテーブルは、他のカードの選択肢から自動的に除外され、同じテーブルを二重に設定できないようにしています
- **計算・ルックアップを含む明細にも対応** — 小計などの計算フィールドや、商品マスターを参照するルックアップ列を含むサブテーブル（見積・注文の明細で一般的な構成）にも、複数行をまとめて正しく流し込めるよう改善しました

## 既存ユーザーの方へ

最新版（v1.1.0）をダウンロードして、kintone の**システム管理 → プラグイン**から上書きアップロードするだけで適用できます。これまでの設定（1つのサブテーブル向けの設定）はそのまま引き継がれ、**再設定は不要**です。

[エクセル一括貼り付け for kintone の製品ページを見る →](/plugins/excel-paste/)
